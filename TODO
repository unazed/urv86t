- tracebacks on logically invalid states, e.g. invalid instructions
  or invalid internal emulator states
- core dumping: register info, surrounding stack bytes, current insn and
  encoding info, etc.
- compressed insn. extension and rv64i support